{
  "name": "supcontent-music",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,green \"npm run dev:api\" \"npm run dev:web\"",
    "dev:all": "npm run dev",
    "dev:api": "node scripts/dev-api-server.mjs",
    "dev:web": "npm --workspace apps/web run dev:static",
    "build": "npm run build:api && npm run build:web",
    "build:api": "npm --workspace apps/api run build",
    "build:web": "npm --workspace apps/web run build",
    "lint:web": "npm --workspace apps/web run lint",
    "test": "npm --workspace apps/api run test && npm --workspace apps/web run test",
    "check": "npm run lint:web && npm run build && npm run test",
    "postinstall": "node scripts/postinstall.mjs"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "framer-motion": "^12.38.0",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
